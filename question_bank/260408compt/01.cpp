#include<bits/stdc++.h>
using namespace std;

int main(void)
{
    double num_card; cin >> num_card;
    vector<double> power(num_card);
    for(double&input:power)
    {
        cin >> input;
    }


    return 0;
}