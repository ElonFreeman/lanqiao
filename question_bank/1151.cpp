#include<bits/stdc++.h>
using namespace std;

int main(void)
{
    long int t=0;
    cin >> t;

    vector<long long> arr(t);
    for(long int i=0;i<t;i++)
    {
        cin >> arr.at(i);
    }
    
    return 0;
}