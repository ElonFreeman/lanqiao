#include<bits/stdc++.h>
using namespace std;

int main(void)
{
    int total,plan;cin >> total >> plan;
    vector<vector<int>> dataset(total,vector<int>(2));
    for(int i=0;i<total;i++)
    {
        cin >> dataset[i][0] >> dataset[i][1];
    }

    

    return 0;
}